resolution: 256
dirs:
  - svbrdfs: test_svbrdfs
    svbrdf_mode: "name/part"
    renders: test_renders_sunny
    texture_gamma: 2.2
    count: 50
loader: !Rendered
  # NOTE: test maps are all rendered at distance 2.0/0.75 = 2.66
  # so that a crop is not needed
  crop: null
  flash_weight: 1.0
  other_weight: !uniform_log_sampled [0.02, 1.5]
